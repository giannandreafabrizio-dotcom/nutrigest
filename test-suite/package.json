{
  "name": "nutrigest-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Suite di test automatica minima per NutriGest (P78) — smoke, unit sulle funzioni pure, render smoke jsPDF.",
  "scripts": {
    "test": "node --test test/**/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "jspdf": "^4.2.1"
  }
}
